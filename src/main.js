// Main loop. I've as least code here as possible. I want all work to be left to the functions.

'use strict';

/*
* Keys are captured and sent to key_press();
*/

window.onkeydown = function(key)
{
	key_press(key);
};

